# Create identity matrix
identity_2d = np.eye(3)              # 3x3 identity matrix
identity_2d_rect = np.eye(3, 4)      # 3x4 rectangular identity matrix
identity_1d = np.eye(3, k=1)         # 3x3 identity matrix with 1s shifted up one row
identity_1d_rect = np.eye(3, 4, k=1) # 3x4 rectangular identity matrix with 1s shifted up one row   